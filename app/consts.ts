import * as path from "node:path";

export const ASSETS_DIR_PATH = path.join(import.meta.dirname!, "../assets");

export const MARKDOWN_DIRECTORY_PATH = path.join(
  import.meta.dirname!,
  "../docs",
);

export const WEBSITE_DOMAIN = "https://pedia.parksb.xyz";
